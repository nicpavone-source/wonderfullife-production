import Link from"next/link";import Card from"@/components/Card";import{createClient}from"@/lib/supabase/server";export default async function Dashboard(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();const{data:profile}=await supabase.from("profiles").select("*").eq("id",user?.id).single();const{data:journey}=await supabase.from("wellness_journeys").select("*").eq("user_id",user?.id).eq("status","active").limit(1).single();return <main className="mx-auto max-w-7xl px-5 py-16 md:px-10"><section className="mb-10 rounded-[2.5rem] bg-white p-10 shadow-wl"><p className="font-black uppercase tracking-[.13em] text-forest">Member Dashboard</p><h1 className="mt-3 font-serif text-5xl md:text-7xl">Welcome back, {profile?.display_name||"friend"}.</h1><p className="mt-5 text-xl text-muted">Your WonderfulLife hub for content, Zoey, and your wellness journey.</p></section><section className="grid gap-6 md:grid-cols-3"><Card title="Wellness Journey" icon="🌿" text={journey?`${journey.title}, day ${journey.current_day}`:"Start your first 30-day journey."}><Link href="/journey" className="mt-5 inline-flex rounded-2xl bg-forest px-5 py-3 font-black text-white">Open Journey</Link></Card><Card title="Ask Zoey" icon="🎙" text="Talk to your friendly AI wellness guide."><Link href="/ask-zoey" className="mt-5 inline-flex rounded-2xl bg-forest px-5 py-3 font-black text-white">Ask Zoey</Link></Card><Card title="Saved Content" icon="🔖" text="Return to saved articles, recipes, and videos."><Link href="/saved" className="mt-5 inline-flex rounded-2xl bg-forest px-5 py-3 font-black text-white">Open Saved</Link></Card></section></main>}
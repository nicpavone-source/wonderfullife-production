import Link from "next/link";import Card from "@/components/Card";import ContentCard from "@/components/ContentCard";import{getFeaturedContent,getPublishedContent}from"@/lib/content";export default async function Home(){const featured=await getFeaturedContent();const latest=await getPublishedContent({limit:6});return <main><section className="mx-auto mt-6 grid max-w-7xl items-center gap-8 rounded-[2.5rem] bg-white p-8 shadow-wl md:p-16 lg:grid-cols-2"><div><p className="font-black uppercase tracking-[.13em] text-forest">WonderfulLife.ca</p><h1 className="mt-4 font-serif text-5xl leading-none md:text-7xl">A wonderful life starts today.</h1><p className="mt-6 text-xl text-muted">Zoey-guided wellness, recipes, videos, daily habits, community, and content publishing in one production-ready platform.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/articles" className="inline-flex h-14 items-center rounded-2xl bg-forest px-7 font-black text-white">Explore Content</Link><Link href="/sign-up" className="inline-flex h-14 items-center rounded-2xl border border-forest/30 bg-white px-7 font-black text-forest">Join Free</Link></div></div><div className="grid place-items-center rounded-[2rem] bg-sage p-10 text-center"><div className="text-8xl">🌿</div><h2 className="mt-5 font-serif text-4xl">Meet Zoey</h2><p className="mt-3 text-muted">Your friendly AI wellness guide.</p></div></section><section className="mx-auto grid max-w-7xl gap-6 px-5 py-16 md:grid-cols-3 md:px-10"><Card icon="📝" title="Articles" text="Publish and browse wellness articles from the CMS."/><Card icon="🥗" title="Recipes" text="Build a healthy recipe library for members."/><Card icon="🎙" title="Zoey AI" text="Ask Zoey for friendly wellness guidance and recommendations."/></section><section className="mx-auto max-w-7xl px-5 pb-16 md:px-10"><h2 className="mb-8 text-center font-serif text-5xl">Featured Content</h2>{featured.length?<div className="grid gap-6 md:grid-cols-3">{featured.map(item=><ContentCard key={item.id} item={item}/>)}</div>:<div className="rounded-[2rem] bg-white p-10 text-center shadow-wl">No featured content yet. Publish content in Studio.</div>}</section><section className="mx-auto max-w-7xl px-5 pb-20 md:px-10"><h2 className="mb-8 text-center font-serif text-5xl">Latest Content</h2>{latest.length?<div className="grid gap-6 md:grid-cols-3">{latest.map(item=><ContentCard key={item.id} item={item}/>)}</div>:<div className="rounded-[2rem] bg-white p-10 text-center shadow-wl">Your public library is ready. Start publishing content in Studio.</div>}</section></main>}